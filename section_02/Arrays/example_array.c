#include <stdio.h>

int main(void)
{
    // We never set the values for any of elements in the array
    int a[4];

    printf("%i\n", a[0]);
    printf("%i\n", a[1]);
    printf("%i\n", a[2]);
    printf("%i\n", a[3]);
}